<div class="photos" id="gallery">     
    {% for thumbnails in collections.thumbnails %}               
        <div class="item active"> 
            <img class="photo active" data-{{thumbnails.data.category}} src="/assets/{{thumbnails.data.name}}.jpg" alt="photo faite par Charles Cantin" >
        </div>
    {% endfor %}
</div>