
<section class="gallery">
  <h1 class="galerieTitle">Galerie photos</h1>
  <ul class="items" id="filters">
    <li class="item active" id="tout">Tout</li>
    <!-- Ajoutez ici d'autres catégories en fonction de vos besoins -->
  </ul>
  <div class="photos" id="gallery">
    {% for thumbnail in collections.thumbnails %}
      <img class="photo active" data-{{thumbnail.data.category}} src="{{thumbnail.data.path}}" alt="photo">
    {% endfor %}
  </div>
</section>

    
